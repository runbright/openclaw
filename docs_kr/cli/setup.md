---
summary: "`openclaw setup` (구성 + 워크스페이스 초기화) CLI 레퍼런스"
read_when:
  - 전체 온보딩 마법사 없이 최초 설정을 할 때
  - 기본 워크스페이스 경로를 설정하고 싶을 때
title: "setup"
---

# `openclaw setup`

`~/.openclaw/openclaw.json` 및 에이전트 워크스페이스를 초기화합니다.

관련 항목:

- 시작하기: [시작하기](/start/getting-started)
- 마법사: [온보딩](/start/onboarding)

## 예시

```bash
openclaw setup
openclaw setup --workspace ~/.openclaw/workspace
```

setup을 통해 마법사를 실행하려면:

```bash
openclaw setup --wizard
```
