---
summary: "`openclaw dashboard` CLI 참조 (Control UI 열기)"
read_when:
  - 현재 토큰으로 Control UI를 열려는 경우
  - 브라우저를 실행하지 않고 URL을 출력하려는 경우
title: "dashboard"
---

# `openclaw dashboard`

현재 인증으로 Control UI를 엽니다.

```bash
openclaw dashboard
openclaw dashboard --no-open
```
