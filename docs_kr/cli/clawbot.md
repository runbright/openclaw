---
summary: "`openclaw clawbot` CLI 참조 (레거시 별칭 네임스페이스)"
read_when:
  - "`openclaw clawbot ...`을 사용하는 이전 스크립트를 유지 관리하는 경우"
  - 현재 명령으로의 마이그레이션 안내가 필요한 경우
title: "clawbot"
---

# `openclaw clawbot`

하위 호환성을 위해 유지되는 레거시 별칭 네임스페이스입니다.

현재 지원되는 별칭:

- `openclaw clawbot qr` ([`openclaw qr`](/cli/qr)과 동일한 동작)

## 마이그레이션

최신 최상위 명령을 직접 사용하는 것이 좋습니다:

- `openclaw clawbot qr` -> `openclaw qr`
